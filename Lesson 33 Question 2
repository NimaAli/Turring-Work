function tri_area ( base, height : real ) : real
    var area : real
    area := base*height/2
    result area 
end tri_area

% main program

var base, height : real
rand ( base ) 
base *= 100
base += 1
rand ( height )
height *= 100
height += 1

put tri_area ( base, height ) : 5 : 5 
